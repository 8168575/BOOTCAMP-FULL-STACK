# TODO - React Day2 (JS conversion)

- [x] Inspect existing Vite+React project structure.
- [x] Create `src/useStateExamples/*` components in JavaScript (JSX).
- [x] Create tabbed UI `src/app.jsx` that renders each example.
- [x] Update `src/main.jsx` to render `src/app.jsx`.
- [ ] Integrate/replace component logic once you provide the exact code for each example file.
- [ ] Verify styling (add/update CSS classes if your provided UI requires it).
- [ ] Run `npm run dev` and `npm run build` to confirm everything compiles.
